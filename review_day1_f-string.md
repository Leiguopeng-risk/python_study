一、基础语法与用法
1. 基础插值（插入变量）
需求：把变量放进字符串里。

写法：f"文字 {变量名} 文字"

风控示例：

python
name = "张三"
income = 15000.5
print(f"客户 {name} 的月收入是 {income} 元")
# 输出：客户 张三 的月收入是 15000.5 元
2. 插入表达式（直接做计算）
需求：不用提前算好，直接在字符串里算。

写法：f"文字 {变量1 运算符 变量2} 文字"

风控示例：

python
income = 15000.5
print(f"客户年收入是 {income * 12} 元")
# 输出：客户年收入是 180006.0 元
3. 调用函数
需求：对变量进行转换或计算后输出。

写法：f"文字 {函数名(变量)} 文字"

风控示例：

python
name = "zhangsan"
print(f"客户姓名大写：{name.upper()}")
# 输出：客户姓名大写：ZHANGSAN
二、进阶格式控制（核心：依据需求自主插入）
这是 f-string 最强大的部分。在 {} 中，变量名后面加一个冒号 :，然后跟上格式控制符。

公式为：{变量 : [填充字符][对齐方式][宽度][,.精度][类型]}

需求 1：控制小数位数（最常用于金额、概率）
需求：只保留两位小数。

格式符：.2f （f 代表浮点数，.2 代表两位小数）

python
income = 15000.567
pd = 0.0234

print(f"月收入：{income:.2f} 元")   # 输出：月收入：15000.57 元
print(f"违约概率：{pd:.2%}")         # 输出：违约概率：2.34%
自主插入思路：看到金额，用 :.2f；看到概率/比率，用 :.2%。

需求 2：添加千分位分隔符（适合大额金额）
需求：1500000 变成 1,500,000。

格式符：,

python
total_loan = 1500000
print(f"贷款总额：{total_loan:,} 元")      # 输出：贷款总额：1,500,000 元
print(f"贷款总额：{total_loan:,.2f} 元")   # 输出：贷款总额：1,500,000.00 元
需求 3：对齐与宽度（适合做表格式报告）
需求：输出整齐对齐的表格，方便阅读。

格式符：< 左对齐，> 右对齐，^ 居中；数字代表总宽度。

python
print(f"{'客户姓名':<10}{'月收入':>12}")
print(f"{'张三':<10}{15000.50:>12.2f}")
print(f"{'李四':<10}{4800.00:>12.2f}")
# 输出：
# 客户姓名            月收入
# 张三              15000.50
# 李四               4800.00
自主插入思路：当你要在终端打印类似 Excel 的表格时，使用 {变量:<宽度} 和 {变量:>宽度}。

需求 4：保留指定有效数字（科学计数法，适合大模型参数量）
格式符：.2e 或 .2g

python
params = 175000000000
print(f"模型参数量：{params:.1e}")  # 输出：模型参数量：1.8e+11
需求 5：日期格式化
需求：把 Python 的日期对象变成“2026-09-23”格式。

格式符：:%Y-%m-%d

python
from datetime import datetime
today = datetime.now()
print(f"报告生成日期：{today:%Y-%m-%d}")
# 输出：报告生成日期：2026-09-23
三、高级用法（风控场景实战）
1. 在 {} 里调用字典键值（处理嵌套数据）
python
customer = {"name": "张三", "income": 15000}
print(f"客户 {customer['name']} 的收入是 {customer['income']} 元")
# 注意：f-string 里引用字典键，要用不同的引号（外双内单，或外单内双）
2. 调试神器：= 号（Python 3.8+）
需求：打印变量时，想同时看到变量名和它的值（常用于排查 bug）。

写法：{变量=}

python
income = 15000.5
overdue = True
print(f"{income=}, {overdue=}")
# 输出：income=15000.5, overdue=True
这在你排查风控逻辑（比如为什么某个客户被判断为高风险）时非常有用。

3. 多行 f-string（生成报告）
python
name = "张三"
income = 15000.5
risk = "低风险"

report = f"""
========= 客户风控报告 =========
客户姓名：{name}
月收入：  {income:,.2f} 元
风险等级：{risk}
==============================
"""
print(report)
四、自主插入的决策流程（遇到需求怎么选）
以后在写代码时，遇到字符串输出，请在大脑里走这个流程：

有变量要插入吗？ → 有，就在字符串前面加 f，变量放在 {} 里。

要计算吗？ → 要，直接在 {} 里写算式（如 {income * 12}）。

要控制小数吗？ → 要，加 :.2f（金额）或 :.2%（概率）。

数字很大要隔开吗？ → 要，加 :,。

要对齐排版吗？ → 要，加 :<10（左对齐10格）或 :>10（右对齐10格）。

是日期吗？ → 是，加 :%Y-%m-%d。

五、常见坑点
引号冲突：f-string 外用了双引号，字典键里面就用单引号。

正确：f"客户 {customer['name']}"

错误：f"客户 {customer["name"]}" （会报语法错误）

花括号转义：如果要在 f-string 里输出真正的 {}，需要写两次：

f"这里的占位符是 {{变量名}}" → 输出：这里的占位符是 {变量名}

不要忘记 f：如果前面忘了加 f，{} 里的内容会被当成普通文字原样输出。

📚 其他常见的转义字符（了解一下）
转义字符	作用	示例	输出
\n	换行	print("A\nB")	A
B
\t	制表符（Tab键）	print("A\tB")	A B
\\	打印一个反斜杠	print("C:\\Users")	C:\Users
\'	打印单引号	print('It\'s')	It's
\"	打印双引号	print("He said \"Hi\"")	He said "Hi"
